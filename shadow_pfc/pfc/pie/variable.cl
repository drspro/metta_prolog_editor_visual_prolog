% Copyright PDC

namespace pfc\pie

class variable : variable

constructors
    new : (string Name).

predicates
    newAnonymous : () -> pie::term Anonymous.

predicates
    normalize : (pie::term Term) -> pie::term Normalize.

end class variable
